# EAVP 项目协作规范

## 语言

- 所有新增或修改的项目文档、ADR、计划、README、面向用户的说明和提交摘要，优先使用简体中文。
- 代码、标识符、协议字段、命令、路径、标准名称、第三方项目名称以及必须保真的引文可保留英文。
- 代码注释应解释设计原因和约束，不重复代码表面行为；新增说明性注释优先使用简体中文。

## 工程约束

- 生产核心保持 C++11 与 POSIX 基线，不在公共头文件中使用 C++14 或更高版本特性。
- 依赖方向固定为 `platform -> control -> media -> base` 和 `management -> base`，跨平面适配只能放在 `platform`。
- 公共 API 不允许异常跨模块边界，失败统一使用 `eavp::Status` 或 `eavp::Result<T>`。
- 新增行为必须先有失败测试，再实现最小代码使其通过；缺陷修复必须包含回归测试。
- 不创建没有行为和验收方式的空模块。真实硬件、协议和产品能力应通过独立规格进入项目。
- 不提交构建目录、本地 Presets、下载缓存、编译产物和运行时转储。

## 文档与提交

- 规范性文档必须声明状态、适用版本和规范性范围。
- ADR 一经接受不得覆盖历史结论；变更决策时新增 ADR 并引用被替代项。
- 提交使用 Conventional Commits 类型前缀和简体中文摘要，例如 `feat(media): 实现有界媒体队列`。
- 每个提交必须是可构建、可测试、可独立审查的交付单元。

