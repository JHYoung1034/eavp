# ADR-0001：核心采用 C++11，插件采用 C ABI

> 状态：已接受  
> 日期：2026-08-18

## 决策

生产核心以 C++11 为语言基线。1.0 前 C++ API 属于实验性接口；未来插件边界使用带版本字段的 C ABI。

## 原因

该组合覆盖较老的 Embedded Linux SoC 工具链，同时允许核心内部使用 RAII、智能指针和模板。C ABI 避免编译器与标准库 ABI 差异扩散到插件。

