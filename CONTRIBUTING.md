# 贡献指南

## 开发流程

1. 从最新基线创建特性分支。
2. 先更新或新增规范性文档，明确接口和验收行为。
3. 对每项行为执行 Red-Green-Refactor：先运行并确认测试因缺少行为而失败，再实现最小代码。
4. 运行 Debug、Release、Sanitizer、安装消费和适用的交叉编译检查。
5. 提交信息使用 Conventional Commits 类型前缀和简体中文摘要。

## 代码要求

- 使用 C++11，公共头文件不得泄漏更高版本语言特性。
- 公共 API 使用 `Status`/`Result<T>` 返回失败，不抛出跨边界异常。
- 所有权必须可从类型看出：独占使用 `std::unique_ptr`，共享媒体存储使用 `std::shared_ptr`，观察关系不得延长生命周期。
- Node 不创建私有线程；执行与调度由 Runtime 统一管理。
- 测试应断言外部可观察行为，不断言实现细节或 mock 本身。

## 文档要求

所有新增或修改文档优先使用简体中文。代码、标识符、协议字段、命令、路径、标准名称和必须保真的引文可保留原文。

